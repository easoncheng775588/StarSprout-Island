package com.example.k12learninggame.repository;

import com.example.k12learninggame.domain.MistakeReviewAttemptEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MistakeReviewAttemptRepository extends JpaRepository<MistakeReviewAttemptEntity, Long> {
}
