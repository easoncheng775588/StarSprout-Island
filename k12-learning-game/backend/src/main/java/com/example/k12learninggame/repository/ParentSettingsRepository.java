package com.example.k12learninggame.repository;

import com.example.k12learninggame.domain.ParentSettingsEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParentSettingsRepository extends JpaRepository<ParentSettingsEntity, Long> {
}
